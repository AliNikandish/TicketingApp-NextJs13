type params={
    params:{
        id:string
    }
}

type TicketCardProps={
    _id:string,
    title:string,
    description:string,
    progress:number,
    priority:number,
    category:string,
    createdAt:string,
    status:string
}